extends ../layout

block content
  main(class="profile-main")
    h1 Profile
    h2= user.username
    button(onclick="deleteProfile()" class="delete-btn delete-profile") Delete

    include ../partials/messages.jade